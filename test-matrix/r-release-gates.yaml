# Group R -- the release gates themselves.
#
# Not product features. They are here because the format's rule is that a test with no row is a
# test somebody wrote without deciding what it promises, and that rule is worth more than the tidy
# boundary it costs. `tier:` is omitted throughout: a gate has no free and no Pro side.

- feature: R1
  name: The release gate refuses to ship on an unmeasured night
  code: scripts/nightly-verdict.mjs
  scenarios:
    - id: R1.1
      scenario: Last night ran and every part passed
      expect: The release proceeds, and the gate prints how old each measurement is rather than only "pass"
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > lets a release through when every part of last night measured and passed"

    - id: R1.2
      scenario: A part found the world broken -- an OCR backend collapsed to empty output, or a metric went catastrophic
      expect: The release is blocked -- the shipped code is already broken against what it was measured against
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks the release when a part found the world broken"

    - id: R1.3
      scenario: A part came back degraded -- slower or worse than baseline, but working
      expect: Reported and never red. A gate that fires on a report teaches the author to ignore red
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > treats a degraded part as a report, not a blocker"

    - id: R1.4
      scenario: The verdict in the tree says pass, but it was written four nights ago
      expect: Blocked. One missed night is an incident, three is a broken schedule, and a verdict nobody refreshed says nothing about today's code
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > refuses a verdict older than three nights, however green it says it is"

    - id: R1.5
      scenario: A release is cut on a Monday after a quiet weekend, on Saturday night's verdict
      expect: It goes through. The window is 72 h precisely so a weekend of GitHub trouble does not need a manual touch
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > still accepts a verdict measured just inside the window, so a quiet weekend does not block a release"

    - id: R1.6
      scenario: A provider answered 429 or 503, or a key expired, on the night before a release
      expect: Not blocked. A quota is not evidence that the plugin is broken, and a gate that fires on what the author did not cause gets overridden until it is ignored
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > does not block on a quota or a 503 that the plugin did not cause"

    - id: R1.7
      scenario: Three nights in a row could not measure, for any reason
      expect: Blocked. However good the excuse, three nights without a measurement is indistinguishable from having no gate at all
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks once nothing has really been measured for three nights, whatever the reason was"

    - id: R1.8
      scenario: A half could not be measured and has never produced a real measurement
      expect: Blocked, and the previous night's number is never carried forward -- a part that could not be measured says so
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > does not let last night's number stand in for a half that could not be measured at all"

    - id: R1.9
      scenario: The nightly wrote some parts and died before the rest
      expect: Blocked. A verdict that measured part of the night is not a verdict, and a lenient parser would read it as a pass
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks when a night measured only some of its parts"

    - id: R1.10
      scenario: Somebody edited the verdict by hand, or a merge left a key behind
      expect: Blocked on the unknown key. Accidental forgery is the one real weakness of a committed status file, and strict parsing is the answer to it
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks on a hand-edited verdict rather than reading it as a pass"

    - id: R1.11
      scenario: A part carries a timestamp that will not parse
      expect: Blocked. Without a readable measuredAt there is no way to tell a fresh verdict from a stale one, so freshness cannot be assumed
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks on a part whose freshness cannot be established"

    - id: R1.12
      scenario: No nightly has ever run -- the state this repo is in today
      expect: "Blocked. The default is fail: \"pass\" has to be written by a run that measured something, and nothing about an absent file can satisfy that"
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > blocks when no verdict file exists at all, which is the state every repo starts in"

    - id: R1.13
      scenario: A newer nightly writes a verdict in a shape this gate predates
      expect: Blocked rather than read optimistically -- a gate that does not understand the file cannot vouch for it
      layer: unit
      file: scripts/nightly-verdict.test.mjs
      test: "judgeVerdict > refuses a verdict written by a newer nightly than this gate understands"

- feature: R2
  name: Render goldens
  code: test-support/golden/extract.ts
  tier: free
  scenarios:
    - id: R2.1
      scenario: The rmscene reference page -- two anchored group nodes, typed text, v1 stroke points
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > normal-a-stroke-2-layers renders exactly its committed golden"

    - id: R2.2
      scenario: The Paper Pro palette page -- SHADER strokes, color_rgba, the extended colors
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > color-and-tool-v3.14.4 renders exactly its committed golden"

    - id: R2.3
      scenario: The page showing a picture -- the image placement the anchor names
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > notebook-with-image renders exactly its committed golden"

    - id: R2.4
      scenario: The real annotated PDF page -- seven highlight runs, margin notes, body marks
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > weather-station-page1 renders exactly its committed golden"

    - id: R2.5
      scenario: The typed-text page -- highlights without start/length, tombstoned runs
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > cape-marrow-typed-highlights renders exactly its committed golden"

    - id: R2.6
      scenario: A single-point tap and a highlighter band -- the disc and the blend no device fixture has
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > synthetic-tap-and-highlight renders exactly its committed golden"

    - id: R2.7
      scenario: A typed heading paragraph -- the heading type size in the text layer
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > synthetic-typed-heading renders exactly its committed golden"

    - id: R2.8
      scenario: Ink past the 6000 px raster clamp -- MAX_RASTER_PX made load-bearing
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > synthetic-runaway-ink renders exactly its committed golden"

    - id: R2.9
      scenario: A node whose tail simply ends -- kept fail-soft, without its placement
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > synthetic-truncated-node renders exactly its committed golden"

    - id: R2.10
      scenario: A node whose tail is malformed mid-field -- the one input that reaches the parser's catch
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > synthetic-corrupt-node-tail renders exactly its committed golden"

    - id: R2.11
      scenario: The same fixture is rendered twice in one process
      expect: Byte-identical goldens, so a diff is always a change and never noise
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > renders byte-identical goldens twice, so a diff is always a change and never noise"

    - id: R2.12
      scenario: The annotated-PDF path -- the seven-highlight page composited onto the PDF it was written on, ink past the paper edge forcing the shrink-to-fit
      expect: Renders exactly its committed golden -- scene, pdf operations, text layer and raster, byte for byte
      layer: unit
      file: test-support/golden/render-goldens.test.ts
      test: "render goldens > weather-station-page1-annotated renders exactly its committed golden"

- feature: R3
  name: The render cluster stays linear
  code: test-support/perf/complexity-ratio.test.ts
  tier: free
  scenarios:
    - id: R3.1
      scenario: renderPagesToPdf at 500 and at 2000 strokes in one process
      expect: The ratio stays under 6x -- linear code measured 3.5-4.2x over ten rounds, and the subtlest injected O(n^2) measured 6.6x, so the limit sits between what is and what must not be
      layer: unit
      file: test-support/perf/complexity-ratio.test.ts
      test: "complexity ratio > renderPagesToPdf stays linear: 4x the strokes may not cost more than 6x the time"

    - id: R3.2
      scenario: rasterizePage at 500 and at 2000 strokes in one process
      expect: The ratio stays under 6x, on every PR on any runner, because the ratio is a property of the code and needs no reference number
      layer: unit
      file: test-support/perf/complexity-ratio.test.ts
      test: "complexity ratio > rasterizePage stays linear: 4x the strokes may not cost more than 6x the time"

# The scripts nothing else runs. `tsc` does not see them as bundles, no test imports them, and the
# only things that reach them are a scheduled nightly and a maintainer with a question -- so a broken
# one reports nothing rather than failing. That is how the 2026-08-25 night came back `unknown`.
- feature: R4
  name: The scripts that bundle still bundle
  code: package.json
  scenarios:
    - id: R4.1
      scenario: A new bundling script is added to package.json
      expect: 'It is checked like the rest. A guard on the guard: were the list ever to come back empty -- a renamed field, a changed shape -- every row below would pass by having nothing left to say'
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > is looking at all of them, so a new one cannot be added without being checked"

    - id: R4.2
      scenario: The nightly's OCR entry point, after an import somewhere under it starts reaching `obsidian`
      expect: 'Caught here rather than by a night that reports nothing. This is the real one: `RENDER_VERSION` came in from `sync-engine`, which reaches `pdf-text` and its `obsidian` import, and `nightly:ocr` was the one esbuild command without `--alias:obsidian`. Nothing was red, the scheduled run simply produced no measurement, and the five nights a baseline needs started counting from further away'
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of nightly:ocr"

    - id: R4.3
      scenario: The nightly's performance entry point
      expect: The same. It measures the renderer, so it reaches further into `src/` than anything else here
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of nightly:perf"

    - id: R4.4
      scenario: The golden updater
      expect: The same. A broken one is found at the worst moment -- while re-recording the goldens a release is about to be judged against
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of goldens:update"

    - id: R4.5
      scenario: The legacy-state freezer
      expect: The same. It is run once per format change and never in between, which is the longest anything here goes unnoticed
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of freeze-state"

    - id: R4.6
      scenario: The `.rm` inspector
      expect: The same. A maintainer reaches for it while debugging a page, which is the moment a broken tool costs the most attention
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of inspect:rm"

    - id: R4.7
      scenario: The cloud document inspector
      expect: The same
      layer: unit
      file: scripts/bundled-scripts.test.mjs
      test: "the scripts that bundle > resolves every import of inspect:doc"
