---
id: 06
trait: formula-notation
language: en
difficulty: hard
writing: >
  Write exactly the characters below, by hand, using real glyphs — a real superscript two, a real
  square-root sign, a real plus-minus sign, a real minus sign rather than a hyphen. Do NOT stack any
  fraction over a horizontal bar: every division here is written with a slash on one line, and that
  is deliberate — a stacked fraction has no single agreed one-line spelling, so it could not be
  compared against a ground truth without measuring our own choice of notation instead of the
  reading.
accept: |
  Two things worth remembering

  $a^2 + b^2 = c^2$

  $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$

  The mean of n values is their sum / n.

  If n doubles, a $O(n^2)$ running time grows by roughly 4×.
---

Two things worth remembering

a² + b² = c²

x = (−b ± √(b² − 4ac)) / 2a

The mean of n values is their sum / n.

If n doubles, a O(n²) running time grows by roughly 4×.
